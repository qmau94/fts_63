<tr>
  <td><%= link_to user.username, [:admin, user] %></td>
  <td><%= user.email %></td>
  <td>
    <%= link_to t("home.edit"), edit_admin_user_path(user) %>
    <%= link_to t("users.delete"), admin_user_path(user), method: :delete,
      data: {confirm: t("confirm")} %>
  </td>
</tr>
